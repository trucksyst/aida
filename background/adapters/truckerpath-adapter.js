/**
 * AIDA v0.1 — TruckerPath Adapter (заглушка)
 * Реализация в следующей версии.
 */

const TruckerpathAdapter = {
    async search(params) {
        console.log('[AIDA/TruckerPath] Adapter not yet implemented');
        return [];
    }
};

export default TruckerpathAdapter;
